#!/usr/bin/env bash
# Preserve the user's invocation directory so frontends (TUI/CLI) can
# operate in arbitrary folders while the project itself stays importable.
export RT_USER_CWD="${RT_USER_CWD:-$PWD}"
cd "$(dirname "$0")"
exec python -m rt_cli "$@"
